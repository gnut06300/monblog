{% extends 'base.html.twig' %}

{% block title %}Liste des articles{% endblock %}

{% block body %}
	<h1>Liste des acticles</h1>

	{% for article in articles %}
		<h2>{{ article.titre }}</h2>
		<div>{{ article.contenu|raw }}</div>
	{% endfor %}
	{{ knp_pagination_render(articles) }}
{% endblock %}
